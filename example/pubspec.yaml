name: popup_menu_title_example
description: A popup menu title example.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  popup_menu_title:
    path: '../'

flutter:
  uses-material-design: true
